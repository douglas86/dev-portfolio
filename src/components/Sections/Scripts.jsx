import useStyles from "./style.jsx";

const Scripts = () => {
  const classes = useStyles();
  return (
    <div id="scripts">
      <h1 className={classes.fonts}>Scripts:</h1>
    </div>
  );
};

export default Scripts;
