import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  home: {
    marginTop: "50px",
  },
  onlineSites: {
    marginTop: "30%",
  },
});

export default useStyles;
